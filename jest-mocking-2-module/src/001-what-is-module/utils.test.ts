import utilsWithDefault, * as utilsWithStar from "./utils";

test("playground", () => {
  console.log("import with * as:", utilsWithStar);
  console.log("import with default:", utilsWithDefault);
});
